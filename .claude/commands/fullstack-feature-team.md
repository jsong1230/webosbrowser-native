---
description: >
  풀스택 기능을 Agent Team + git worktree 병렬 파이프라인으로 구현합니다.
  분석/설계/계획 → worktree 생성 → 병렬 구현 → merge → 검증 → 문서화 → 커밋
---

## Phase 0: 기능명 확인
- 사용자 요청에서 기능명(영문 kebab-case) 결정
- docs/specs/{기능명}/ 디렉토리 생성
- /next-feature에서 호출된 경우 features.md 상태를 🔄 진행중으로 변경

## Phase 1: 분석 + 설계 + 계획 (서브에이전트 — 순차)

### Step 1: 요구사항 분석 (product-manager)
- docs/specs/{기능명}/requirements.md 작성

### Step 2: 기술 설계 (architect)
- 기존 코드 분석 + design.md 작성
- API 설계, DB 설계, 시퀀스 흐름, 아키텍처 결정

### Step 3: 구현 계획 (product-manager)
- design.md 기반으로 plan.md 작성
- 태스크 분해, 의존성, 병렬 실행 판단

## Phase 2: Worktree 세팅
```
bash .claude/scripts/worktree-setup.sh {기능명} backend frontend
```

## Phase 3: 구현 + 기술 문서 (Agent Team — 병렬)
팀을 생성하고 팀원을 spawn합니다.

### 팀원 구성
| 팀원      | subagent_type | 작업 디렉토리                    | 참조 문서          | 작성 문서          |
|-----------|---------------|--------------------------------|-------------------|-------------------|
| backend   | backend-dev   | .worktrees/{기능명}-backend/    | design.md, plan.md | docs/api/, docs/db/ |
| frontend  | frontend-dev  | .worktrees/{기능명}-frontend/   | design.md, plan.md | docs/components/   |

### spawn 프롬프트에 포함할 내용
- docs/specs/{기능명}/ 의 세 문서 내용 (requirements, design, plan)
- "반드시 .worktrees/{기능명}-{팀원명}/ 에서 작업"
- "설계서(design.md)를 따라 구현하되, 변경 필요 시 팀 리더에게 보고"

### 팀원 간 통신
- backend 완료 → frontend에게 API 스키마 + 설계 대비 변경사항 공유
- 설계 변경 필요 시 → 팀 리더 → architect에게 design.md 업데이트 요청

## Phase 4: Worktree 머지
```
bash .claude/scripts/worktree-merge.sh {기능명} backend frontend
```

## Phase 5: 검증 (서브에이전트 — 순차, main 브랜치)
1. test-runner: requirements.md + design.md 기반 테스트
2. code-reviewer: 코드 + 설계↔구현 일치 + 문서 리뷰
3. 이슈 수정 루프 (이슈가 있는 경우에만):
   - Critical 이슈 → 해당 에이전트로 코드 수정 → test-runner 재실행
   - 문서 불일치 → 해당 에이전트로 문서 수정

## Phase 6: 운영 문서 + 커밋
1. doc-writer: 진행 로그 + CHANGELOG
2. Git 커밋: 설계문서 / 구현코드 / 기술문서 / 운영문서 분리

## Phase 7: 최종 보고

delegate mode 사용. 사용자 요청: $ARGUMENTS
