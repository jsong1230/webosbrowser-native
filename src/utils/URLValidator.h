/**
 * @file URLValidator.h
 * @brief URL 검증 유틸리티 헤더 (스켈레톤)
 */

#pragma once

namespace webosbrowser {

class URLValidator {
public:
    URLValidator() = default;
    ~URLValidator() = default;

    // TODO: URL 검증 기능 구현
};

} // namespace webosbrowser
